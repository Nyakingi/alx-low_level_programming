#include "main.h"
/**
 * abs - computes the absolute value of an integer
 * @t: The integer to be computed
 * Return: The absolute value
 */
int abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
